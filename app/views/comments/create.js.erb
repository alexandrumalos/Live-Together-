$('#comments-<%=j @post.id.to_s %>').html("<% @post.comments.each do |comment| %><%=j render comment %><% end %>");

$('#comments-count-<%=j @post.id.to_s %>').html("<%=j comments_button(@post) %>");

$('#comments-form-<%=j @post.id.to_s %>').val('');
